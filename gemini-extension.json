{
  "name": "stupidloud-codegraph",
  "version": "0.9.9",
  "description": "Supercharge your agent with semantic code intelligence. 94% fewer tool calls • 77% faster exploration • 100% local.",
  "settings": [
    {
      "name": "No Watch",
      "description": "Disable file watcher (set to '1' to disable, useful for slow filesystems like WSL2 /mnt)",
      "envVar": "CODEGRAPH_NO_WATCH"
    }
  ],
  "mcpServers": {
    "codegraph": {
      "command": "npx",
      "args": ["codegraph", "serve", "--mcp"],
      "cwd": "${extensionPath}"
    }
  },
  "contextFileName": "GEMINI.md",
  "commands": {
    "cg": {
      "init": {
        "description": "Initialize CodeGraph in the current project",
        "shell": "npx codegraph init --index"
      },
      "status": {
        "description": "Show CodeGraph index status",
        "shell": "npx codegraph status"
      },
      "ui": {
        "description": "Open CodeGraph interactive visualization",
        "shell": "npx codegraph visualize"
      }
    }
  }
}
